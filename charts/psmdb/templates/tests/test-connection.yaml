apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "psmdb.fullname" . }}-test-connection"
  labels:
    {{- include "psmdb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "psmdb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
